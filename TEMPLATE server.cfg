# # # # # # # # # # # # # # # # # # # # # # # # # # #
#    ___ _____  __  _    ___ ___   _   _____   __   #
#   | __/ __\ \/ / | |  | __/ __| /_\ / __\ \ / /   #
#   | _|\__ \>  <  | |__| _| (_ |/ _ \ (__ \ V /    #
#   |___|___/_/\_\ |____|___\___/_/ \_\___| |_|     #
#                                                   #
#     Discord: https://discord.gg/esx-framework     #
#     Website: https://esx-framework.org/           #
# # # # # # # # # # # # # # # # # # # # # # # # # # #

# Only change the IP if you're using a server with multiple network interfaces, otherwise change the port only.
endpoint_add_tcp "0.0.0.0:30120"
endpoint_add_udp "0.0.0.0:30120"

### Endpoints Privacy ###
sv_endpointPrivacy true

sv_hostname "HIER SERVER NAMEN EINTRAGEN"
set steam_webApiKey "HIER STEAM API KEY EINTRAGEN"
sv_licenseKey "HIER CFX.RE LICENSE KEY EINTRAGEN"
sv_maxclients 10   # Allow access to features usually locked behind a FiveM patreon key

sets sv_projectName "HIER SERVER NAMEN EINTRAGEN"
sets sv_projectDesc "Hier Server Beschreibung eintragen"
sets tags "default, esx, esx legacy, legacy, official, roleplay"
sv_scriptHookAllowed 0

## [txAdmin CFG validator]: onesync MUST only be set in the txAdmin settings page.
# set onesync on  # Enable OneSync [REQUIRED FOR LEGACY 1.7.5 +]
set mysql_ui true
set mysql_connection_string "server=SERVER IP EINTRAGEN;database=DATENBANK NAME EINTRAGEN;userid=USER ID EINTRAGEN;password=PASSWORT EINTRAGEN"

## These resources will start by default.
ensure [CFX]
ensure [ESX]

## Add system admins
add_principal group.admin group.user
add_ace group.admin command allow # allow all commands
add_ace group.admin command.quit deny # but don't allow quit
add_ace resource.es_extended command.add_ace allow
add_ace resource.es_extended command.add_principal allow
add_ace resource.es_extended command.remove_principal allow
add_ace resource.es_extended command.stop allow

